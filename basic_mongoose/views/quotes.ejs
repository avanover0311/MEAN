<!DOCTYPE html>
<html>
<head>
	<title>Quotes</title>
</head>
<body>
	<div class="container">
		  <% for(i in quotes) { %>
                <li class="quote"><%= quotes[i].quote %></li>
                <li class="quote"><%= quotes[i].name %> at <%= moment(quotes[i].createdAt).format('h:mm a, MMMM Do YYYY') %></li>
              <% } %>
	</div>
</body>
</html>